# Career Pilot AI

## Overview

Career Pilot AI is a static website for a career guidance service. It includes pages for home, about, services, FAQ, contact, login, signup, and a chatbot demo.

## Team

- Ayyan
- Owais
- Abdullah

## Project Structure

- `index.html` - Homepage
- `about.html` - About page
- `services.html` - Services page
- `faq.html` - FAQ page
- `contact.html` - Contact page
- `login.html` - Login page
- `signup.html` - Signup page
- `chatbot.html` - Chatbot interface
- `EXAMPLE.html` - Example/demo page
- `style.css` - Global website styling
- `chatbot.css` - Chatbot-specific styling
- Page-specific CSS files for each page: `about.css`, `contact.css`, `faq.css`, `services.css`, `login.css`, `signup.css`

## How to Use

1. Open the HTML files directly in a browser.
2. Edit content in the HTML files as needed.
3. Customize the theme and layout by updating `style.css` and page-specific CSS files.
4. For chatbot styling, update `chatbot.css`.

## Chatbot

- `chatbot.html` provides an interactive chatbot interface for users.
- `chatbot (1).js` contains the chatbot logic and message handling.
- `chatbot.css` styles the chat window, bubbles, and input area.
- You can enhance the chatbot by connecting `chatbot (1).js` to an AI service or backend endpoint for dynamic responses.

## Notes

- The project is built with plain HTML and CSS.
- No build tools or dependencies are required.
- Use this repository as a base for a simple career guidance landing site.

## Recommended Improvements

- Add real backend support for login/signup and contact form handling.
- Make the chatbot interactive by connecting `chatbot.js` to an AI service.
- Improve accessibility, responsiveness, and mobile layout.

## GitHub Upload

- This README and project files are now committed and pushed to the `main` branch on GitHub.
- The repository is synchronized with the remote.

